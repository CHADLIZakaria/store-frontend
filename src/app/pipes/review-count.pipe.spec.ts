import { ReviewCountPipe } from './review-count.pipe';

describe('ReviewCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewCountPipe();
    expect(pipe).toBeTruthy();
  });
});
