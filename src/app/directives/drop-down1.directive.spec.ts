import { DropDown1Directive } from './drop-down1.directive';

describe('DropDown1Directive', () => {
  it('should create an instance', () => {
    const directive = new DropDown1Directive();
    expect(directive).toBeTruthy();
  });
});
